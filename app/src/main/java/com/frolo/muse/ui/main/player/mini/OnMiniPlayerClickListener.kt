package com.frolo.muse.ui.main.player.mini


/**
 * Callback that fires when MiniPlayer's layout is clicked.
 */
interface OnMiniPlayerClickListener {
    fun onMiniPlayerLayoutClick(fragment: MiniPlayerFragment)
}